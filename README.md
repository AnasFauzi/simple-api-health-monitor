# Simple API Health Monitor

A lightweight Python tool for checking website and API availability, HTTP status, and response time.

## Features

- Check whether a URL is online
- Display HTTP status codes
- Measure response time
- Handle connection errors and timeouts
- Simple command-line interface

## Requirements

- Python 3.10 or newer
- `requests`

## Installation

Clone the repository:

```bash
git clone https://github.com/AnasFauzi/simple-api-health-monitor.git
cd simple-api-health-monitor
